<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidfinalproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".NewsFeed_Search"></activity>
        <activity android:name=".NewsFeed_Saved" />
        <activity android:name=".NewsFeed_Star" />

        <activity android:name=".NYT_search_result"></activity>
        <activity android:name=".NYT_savedArticles" />

        <activity android:name=".FlightStatusFlightInformation" />
        <activity android:name=".FlightStatusHome" />
        <activity android:name=".NewsFeed_MainActivity" /> <!-- activity android:name=".ArticleSearchList"></activity -->
        <activity android:name=".FullArticle" />

        <activity android:name=".FullArticleSaved" />

        <activity android:name=".NYTimes_MainActivity" />
        <activity android:name=".FinalProject_MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NewsFeed_Detail" />
        <activity android:name=".Dictionary_MainActivity"></activity>
        <activity android:name=".Dictionary_SearchActivity"></activity>
        <activity android:name=".Dictionary_Element"></activity>
    </application>

</manifest>